# PostgreSQL environment variables (used by debugger)
# For local development, use localhost. Docker containers use 'db' hostname.
PGHOST=localhost
PGPORT=5432
PGDATABASE=projectdb
PGUSER=testuser
PGPASSWORD=example